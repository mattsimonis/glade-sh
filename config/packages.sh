#!/bin/bash
# ── Roost — custom packages ──────────────────────────────────────────────────
# This script runs at Docker build time. Add any packages, CLIs, or extensions
# you want baked into the image. Runs as root.
#
# Rebuild after editing:  make build
#
# See config/packages.sh.example for common recipes.
# ─────────────────────────────────────────────────────────────────────────────
